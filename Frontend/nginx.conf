# Configuración de nginx para servir la SPA de Angular
# try_files hace que las rutas internas (/welcome, /users...) carguen index.html
server {
  listen 80;
  server_name localhost;
  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }
}
